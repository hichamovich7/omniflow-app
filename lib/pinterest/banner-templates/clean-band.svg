<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1024 140">
  <rect x="0" y="0" width="1024" height="140" fill="{{ACCENT_COLOR}}" />
  <text
    x="512"
    y="70"
    text-anchor="middle"
    dominant-baseline="central"
    font-family="Arial, Helvetica, sans-serif"
    font-weight="700"
    font-size="{{FONT_SIZE}}"
    fill="{{TEXT_COLOR}}"
  >{{TEXT}}</text>
</svg>
